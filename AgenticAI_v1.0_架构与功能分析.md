# AgenticAI v1.0 - 项目架构与功能分析

## 📋 项目概述

**AgenticAI v1.0** 是一个基于 LangGraph 的多智能体协作系统，支持 Web 搜索、深度思考、会话记忆和流式输出功能。该系统采用模块化设计，通过多个专业化 Agent 协同工作来处理用户查询。

---

## 🏗️ 项目架构

### 1️⃣ 整体架构层次

```
┌─────────────────────────────────────────────────────────────┐
│                      用户界面层 (UI Layer)                    │
│  • Streamlit Web 界面 (app.py)                               │
│  • 侧边栏组件 (ui/sidebar.py)                                │
│  • 聊天组件 (ui/components.py)                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   核心编排层 (Orchestration)                  │
│  • Pipeline 编排器 (core/pipeline.py)                        │
│  • LangGraph Workflow 构建器 (workflows/builder.py)          │
│  • 路由规则 (workflows/routers.py)                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                     智能体层 (Agents Layer)                   │
│  ┌──────────────┬──────────────┬──────────────────────────┐ │
│  │ Understanding│ Web Search   │ Initial Analysis         │ │
│  │   Agent      │   Agent      │      Agent               │ │
│  ├──────────────┼──────────────┼──────────────────────────┤ │
│  │ Reflection   │ Detailed     │ Code Generation          │ │
│  │   Agent      │  Analysis    │      Agent               │ │
│  │              │   Agent      │                          │ │
│  ├──────────────┴──────────────┴──────────────────────────┤ │
│  │            Synthesis Agent (整合输出)                    │ │
│  └──────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    数据层 (Data Layer)                        │
│  • SQLite 数据库 (database/manager.py)                       │
│  • 会话管理 (database/session.py)                            │
│  • 数据加密 (utils/crypto.py)                                │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   外部服务层 (External Services)              │
│  • Azure OpenAI (LLM 服务)                                   │
│  • Tavily API (Web 搜索服务)                                 │
└─────────────────────────────────────────────────────────────┘
```

---

### 2️⃣ 目录结构详解

```
agenticai-v1-0/
├── agents/                          # 智能体模块
│   ├── base.py                      # 基础 Agent 类 (LLM 初始化)
│   ├── understanding.py             # 意图理解 Agent
│   ├── search.py                    # Web 搜索 Agent
│   ├── analysis.py                  # 分析 Agent (初步+详细)
│   ├── reflection.py                # 反思 Agent
│   ├── code_generator.py            # 代码生成 Agent
│   └── synthesis.py                 # 综合输出 Agent
│
├── core/                            # 核心逻辑
│   ├── models.py                    # 数据模型 (Pydantic)
│   └── pipeline.py                  # Pipeline 编排器
│
├── workflows/                       # 工作流编排
│   ├── builder.py                   # LangGraph Workflow 构建
│   └── routers.py                   # 路由决策逻辑
│
├── config/                          # 配置模块
│   ├── settings.py                  # 系统配置 (Azure, Tavily)
│   ├── language_styles.py           # 多语言 Prompt 模板
│   └── .encryption_key              # 加密密钥
│
├── database/                        # 数据持久化
│   ├── manager.py                   # 数据库管理器
│   ├── session.py                   # 会话管理器
│   └── db/                          # SQLite 数据库文件
│
├── prompts/                         # Prompt 管理 (新架构)
│   ├── registry.py                  # Prompt 注册中心
│   ├── composer.py                  # Prompt 组合器
│   └── system/                      # 系统角色 Prompt
│       └── analyst.py               # 分析师角色定义
│
├── ui/                              # 用户界面
│   ├── sidebar.py                   # 侧边栏组件
│   └── components.py                # 通用 UI 组件
│
├── utils/                           # 工具模块
│   ├── crypto.py                    # 内容加密器
│   └── logger.py                    # 日志配置
│
├── app.py                           # Streamlit 主应用
├── main.py                          # CLI 入口
└── requirements.txt                 # 依赖清单
```

---

## ⚙️ 核心功能模块

### 1️⃣ **智能体系统 (Multi-Agent System)**

#### 📌 Understanding Agent (意图理解)
- **功能**: 分析用户查询，识别意图、领域、关键概念
- **输出**: 
  - `intent`: 用户意图
  - `domain`: 领域分类 (general / Arch/DEV / medical / legal)
  - `requires_web_search`: 是否需要联网搜索
  - `requires_code`: 是否需要代码生成
  - `key_concepts`: 关键技术概念
- **特性**: 支持 JSON 容错解析,避免内容过滤错误

#### 📌 Web Search Agent
- **功能**: 通过 Tavily API 进行实时网络搜索
- **触发条件**: 
  - Medical/Legal 领域自动触发
  - 用户显式启用 Web Search
  - Understanding Agent 判断需要最新信息
- **输出**: 搜索结果摘要 + 原始结果列表

#### 📌 Initial Analysis Agent
- **功能**: 基于理解结果和搜索结果进行初步分析
- **模式**: 
  - **同步模式**: `analyze()` - 一次性返回
  - **流式模式**: `analyze_streaming()` - 增量输出
- **特性**: 支持对话历史上下文记忆 (最近 10 轮)

#### 📌 Reflection Agent (深度思考)
- **功能**: 自我反思,批判性评估初步分析
- **输出**: 
  - `strengths`: 分析优势
  - `weaknesses`: 潜在缺陷
  - `improvements`: 改进建议
  - `refined_answer`: 优化后的答案
- **触发条件**: 用户启用 Deep Thinking Mode

#### 📌 Detailed Analysis Agent
- **功能**: 为技术任务提供详细分析
- **输出**: 
  - `requirements`: 需求清单
  - `architecture`: 系统架构描述
  - `tech_stack`: 技术栈推荐
  - `clarifications`: 需要澄清的问题
- **触发条件**: Arch/DEV 领域 + `requires_code=true`

#### 📌 Code Generation Agent
- **功能**: 生成生产级代码
- **输出**: `CodeArtifact` 对象
  - `title`: 代码标题
  - `language`: 编程语言
  - `code`: 代码内容
  - `explanation`: 代码说明
  - `dependencies`: 依赖项
- **特性**: 使用专用 Coder Model (o4-mini)

#### 📌 Synthesis Agent (综合输出)
- **功能**: 整合所有 Agent 的结果,生成最终答案
- **输出格式**: 
  - 需求理解
  - 网络搜索结果
  - 分析结果 / 反思优化
  - 技术方案
  - 代码实现
  - 领域免责声明

---

### 2️⃣ **工作流编排 (Workflow Orchestration)**

#### 📌 LangGraph Workflow
```
用户查询
    ↓
Understanding Agent ───→ 识别领域/意图
    ↓
   判断
    ├─→ [Medical/Legal] → Web Search → Initial Analysis
    ├─→ [Web Search Mode] → Web Search → Initial Analysis
    └─→ [Basic Mode] → Initial Analysis
    ↓
   判断
    ├─→ [Deep Thinking] → Reflection → (判断)
    ├─→ [Requires Code] → Detailed Analysis → Code Generation
    └─→ [Default] → Synthesis
    ↓
Synthesis Agent ───→ 最终答案
```

#### 📌 路由规则 (Routers)
- **`route_after_understanding`**: 根据领域和模式决定下一步
- **`route_after_initial_analysis`**: 决定是否需要反思/代码生成
- **`route_after_reflection`**: 反思后决定是否需要代码
- **`route_after_detailed_analysis`**: 决定是否生成代码

---

### 3️⃣ **数据持久化 (Persistence Layer)**

#### 📌 SQLite 数据库
**表结构**:
```sql
-- 会话表
sessions (
  session_id TEXT PRIMARY KEY,
  title TEXT,
  domain TEXT,
  language TEXT,
  status TEXT,              -- active / deleted (逻辑删除)
  created_at REAL,
  updated_at REAL,
  summary TEXT              -- 自动摘要
)

-- 消息表
messages (
  id INTEGER PRIMARY KEY,
  session_id TEXT,
  role TEXT,                -- user / assistant
  content TEXT,             -- 加密存储
  metadata TEXT,            -- 加密存储 (JSON)
  created_at REAL
)

-- 代码工件表
artifacts (
  id INTEGER PRIMARY KEY,
  session_id TEXT,
  artifact_type TEXT,       -- code
  title TEXT,
  content TEXT,             -- 加密存储
  language TEXT,
  created_at REAL
)
```

#### 📌 数据加密
- **加密算法**: Fernet (对称加密)
- **加密内容**: 
  - 消息内容 (`messages.content`)
  - 元数据 (`messages.metadata`)
  - 代码内容 (`artifacts.content`)
- **密钥管理**: 存储在 `config/.encryption_key`

#### 📌 会话管理
- **自动摘要**: 基于首条用户消息生成
- **逻辑删除**: 标记为 `deleted` 而非物理删除
- **审计功能**: 可查看已删除会话历史

---

### 4️⃣ **Prompt 管理系统 (新架构)**

#### 📌 Prompt Registry (注册中心)
- **功能**: 集中管理所有 Prompt 模板
- **分类**: 
  - `system`: 系统角色 (analyst, coder, base)
  - `tasks`: 任务定义 (understanding, code_generation)
  - `tools`: 工具说明
  - `skills`: 推理技能
  - `preferences`: 用户偏好
- **特性**: 
  - 向后兼容 v1.0 的 `LANGUAGE_STYLES`
  - 动态加载 `prompts/` 目录下的模板
  - 多语言支持 (中文, English, 日本語)

#### 📌 Prompt Composer (组合器)
- **功能**: 将多层 Prompt 组合成完整系统提示词
- **组合层次**: 
  1. System Role (角色定义)
  2. Task Definition (任务描述)
  3. Available Tools (可用工具)
  4. Reasoning Skills (推理技能)
  5. User Preferences (用户偏好)

---

### 5️⃣ **用户界面 (Streamlit UI)**

#### 📌 主界面功能
- **聊天界面**: 
  - 消息历史展示
  - 流式输出 (实时显示生成过程)
  - 状态指示器 (理解中、搜索中、分析中等)
- **样式特性**: 
  - 渐变色主题 (紫色系)
  - 现代化卡片设计
  - 响应式布局
  - 自定义滚动条

#### 📌 侧边栏功能
- **处理选项**: 
  - 🧠 Deep Thinking Mode (链式思考推理)
  - 🌐 Web Search (实时网络检索)
- **会话管理**: 
  - ➕ 新建会话
  - 📂 加载历史会话
  - 🗑️ 删除会话 (双重确认)
  - 🗂️ 查看已删除会话 (审计)
- **会话显示**: 
  - 💬 会话摘要 (自动生成)
  - 📅 更新时间 (今天显示时分,其他显示日期)
  - 🏷️ 领域标签

---

## 🔄 处理流程模式

### 1️⃣ **Basic Mode (基础模式)**
```
Understanding → Analysis → Synthesis
```
- 适用场景: 普通问答、信息查询
- 处理速度: 最快
- 适合: 事实性问题、简单咨询

### 2️⃣ **Deep Thinking Mode (深度思考)**
```
Understanding → Analysis → Reflection → Synthesis
```
- 适用场景: 需要深度推理的复杂问题
- 特点: 自我批判、迭代优化
- 适合: 技术方案设计、复杂决策

### 3️⃣ **Web Search Mode (联网搜索)**
```
Understanding → Web Search → Analysis → Synthesis
```
- 适用场景: 需要最新信息
- 触发条件: 
  - 用户主动启用
  - Medical/Legal 领域自动触发
- 适合: 时事新闻、最新技术动态

### 4️⃣ **Code Generation Mode (代码生成)**
```
Understanding → Analysis → Detailed Analysis → Code Generation → Synthesis
```
- 适用场景: Arch/DEV 领域编程任务
- 自动触发: `domain=Arch/DEV` + `requires_code=true`
- 输出: 完整代码 + 说明 + 依赖项

---

## 🌐 多语言支持

### 支持语言
- 🇨🇳 **中文**: 默认语言
- 🇺🇸 **English**: 英语
- 🇯🇵 **日本語**: 日语

### 语言配置
- **系统 Prompt**: 每种语言独立模板
- **免责声明**: 
  - Medical Disclaimer (医疗免责)
  - Legal Disclaimer (法律免责)
- **UI 文案**: 自适应语言切换

---

## 🔒 安全与隐私

### 数据加密
- **加密对象**: 
  - 聊天消息
  - 元数据
  - 代码工件
- **加密算法**: Fernet (AES-128)
- **密钥存储**: 本地文件 (生产环境应使用密钥管理服务)

### 内容安全
- **Azure OpenAI**: 内置 Responsible AI 过滤器
- **错误处理**: 检测并提示内容安全策略触发

### 审计功能
- **逻辑删除**: 会话数据保留用于审计
- **操作日志**: 完整的执行日志记录

---

## 📊 技术栈总结

### 后端框架
- **LangChain**: LLM 编排框架
- **LangGraph**: 状态图工作流引擎
- **Pydantic**: 数据模型验证

### LLM 服务
- **Azure OpenAI**: 
  - Analyst Model (通用分析)
  - Coder Model (代码生成, o4-mini)

### 外部 API
- **Tavily**: Web 搜索服务

### 数据存储
- **SQLite**: 轻量级关系数据库
- **Cryptography**: Fernet 加密

### 前端界面
- **Streamlit**: Web UI 框架
- **Custom CSS**: 现代化样式设计

### 工具库
- **python-dotenv**: 环境变量管理
- **asyncio/aiohttp**: 异步流式处理

---

## 🎯 关键特性亮点

### ✅ 已实现的高级功能
1. **多 Agent 协作**: 7 个专业化 Agent 协同工作
2. **流式输出**: 实时展示生成过程,提升用户体验
3. **对话记忆**: 最近 10 轮对话上下文感知
4. **深度思考**: Chain-of-Thought 自我反思机制
5. **代码生成**: 生产级代码 + 完整说明
6. **数据加密**: 端到端加密存储
7. **会话管理**: 完整的 CRUD + 审计功能
8. **多语言**: 3 种语言界面和 Prompt

### 🚀 架构优势
1. **模块化设计**: 高内聚低耦合
2. **可扩展性**: 易于添加新 Agent
3. **容错机制**: 多层 JSON 解析容错
4. **向后兼容**: 新旧 Prompt 系统共存

---

## 📝 配置要求

### 环境变量 (.env)
```env
# Azure OpenAI
AZURE_OPENAI_API_KEY=your_api_key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=your-analyst-model
AZURE_OPENAI_O4_MINI_DEPLOYMENT=your-coder-model

# Tavily (可选)
TAVILY_API_KEY=your_tavily_key
```

### 依赖安装
```bash
pip install -r requirements.txt
```

### 启动应用
```bash
streamlit run app.py
```

---

## 🎨 总结

**AgenticAI v1.0** 是一个功能完整、架构清晰的多智能体系统,具备:
- ✅ 7 个专业化 Agent 协同工作
- ✅ LangGraph 编排引擎
- ✅ 流式输出 + 对话记忆
- ✅ 多模式处理 (Basic / Deep Thinking / Web Search / Code Gen)
- ✅ 数据加密 + 会话管理
- ✅ 现代化 Streamlit UI
- ✅ 多语言支持
- ✅ 可扩展的 Prompt 管理系统

该项目为构建智能对话助手提供了坚实的基础架构! 🚀
